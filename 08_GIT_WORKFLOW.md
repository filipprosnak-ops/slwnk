# Git Workflow

Always commit after each feature.

Example commits:

init plugin
add seasons CPT
add teams CPT
add players CPT
add games CPT
add standings service

Never generate large changes without committing first.